plugins {
    id 'java-library'
    id 'java-conventions'
}

group 'io.github.gabrielmsouza.catalogo.domain'

dependencies {
    implementation(project(":domain"))

    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}